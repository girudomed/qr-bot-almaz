################################################################################
#  HTTP  →  внеш. порт 8081   (домен + порт)
################################################################################
your-domain.example.com:8081 {
	# 🔄 /health → 200  — чтобы было живо, но не мешало аппу
	@health path /health
	respond @health 200

	encode gzip zstd
	reverse_proxy qrbot:8080
}

################################################################################
#  HTTPS →  внеш. порт 8443   (домен + порт + self-signed TLS)
################################################################################
your-domain.example.com:8443 {
	# самоподписанный сертификат; замените на  tls you@mail  при LE
	tls internal

	@health path /health
	respond @health 200

	encode gzip zstd
	reverse_proxy qrbot:8080
}